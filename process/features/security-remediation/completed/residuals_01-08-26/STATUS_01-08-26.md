# Residuals closed — 01-08-26

These notes were accepted Phase-1/2 residuals; code is now in place:

| Note | Resolution |
|------|------------|
| oauth-state-binding-hardening | `/api/fyers/login_url` sets verifier cookie; UI `connectFyers()` opens `/fyers/auth` |
| phase1-square-off-reentry-guard | `TradingState.square_off_in_progress` blocks toggle + Telegram `/start` + `can_trade` |
| exit-gate-line-window-fragility | `tests/test_order_lock_record_trade_ast.py` replaces fragile grep window |

Original notes kept below this folder for audit trail.
